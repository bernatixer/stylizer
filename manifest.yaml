apiVersion: apps/v1
kind: Deployment
metadata:
  name: stylizer
spec:
  replicas: 1
  selector:
    matchLabels:
      role: stylizer
  template:
    metadata:
      labels:
        role: stylizer
    spec:
      containers:
      - name: stylizer
        image: bernatixer/stylizer:v0.0.6
        ports:
        - containerPort: 8080
      imagePullSecrets:
        - name: dockerreg

---
apiVersion: v1
kind: Service
metadata:
  name: stylizer
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30000
  selector:
    role: stylizer
