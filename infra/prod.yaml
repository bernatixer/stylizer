namespace: default
 
env:
  HTTP_ADDR: :5000
 
image:
  name: bernatixer/stylizer
  tag: latest
  pull: IfNotPresent
 
deployment:
  force: false
  replicas: 1
  container:
    name: stylizer
    port: 5000
 
service:
  type: NodePort
  port: 5000
  nodePort: 30000
